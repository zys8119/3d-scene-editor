import { commonLight } from '../../_styles/common';
export const self = (vars) => {
    return {
        padding: '10px',
    };
};
const notificationBarLight = {
    name: 'NotificationBar',
    common: commonLight,
    self,
};
export default notificationBarLight;
