import { commonLight } from '../../../_styles/common';
export const self = (vars) => {
    return {
        padding: '10px',
    };
};
const proFormLight = {
    name: 'ProForm',
    common: commonLight,
    self,
};
export default proFormLight;
