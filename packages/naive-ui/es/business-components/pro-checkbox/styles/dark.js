import { commonDark } from '../../../_styles/common';
import { self } from './light';
const proCheckboxDark = {
    name: 'ProCheckbox',
    common: commonDark,
    self(vars) {
        return self(vars);
    },
};
export default proCheckboxDark;
