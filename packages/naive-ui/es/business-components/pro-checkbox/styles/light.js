import { commonLight } from '../../../_styles/common';
export const self = (vars) => {
    return {
        padding: '10px',
    };
};
const proCheckboxLight = {
    name: 'ProCheckbox',
    common: commonLight,
    self,
};
export default proCheckboxLight;
