import { commonLight } from '../../../_styles/common';
export const self = (vars) => {
    return {
        padding: '10px',
    };
};
const mapLight = {
    name: 'Map',
    common: commonLight,
    self,
};
export default mapLight;
