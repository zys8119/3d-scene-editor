import { commonLight } from '../../../_styles/common';
const self = (vars) => {
    return {};
};
const rotateAlbumLight = {
    name: 'RotateAlbum',
    common: commonLight,
    self,
};
export default rotateAlbumLight;
