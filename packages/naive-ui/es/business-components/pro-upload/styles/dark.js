import { commonDark } from '../../../_styles/common';
import { self } from './light';
const proUploadDark = {
    name: 'ProUpload',
    common: commonDark,
    self(vars) {
        return self(vars);
    },
};
export default proUploadDark;
