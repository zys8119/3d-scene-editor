import { commonDark } from '../../../_styles/common';
import { self } from './light';
const searchTablePageDark = {
    name: 'SearchTablePage',
    common: commonDark,
    self(vars) {
        return self(vars);
    },
};
export default searchTablePageDark;
